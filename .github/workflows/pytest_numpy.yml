name: Run Pytest (numpy)
# Run the test suite using pytest

on:
  # pushes to main
  push:
    branches:
      - main
  # PRs
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]


jobs:
  build:
    if: github.event.pull_request.draft == false

    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      # make pytest output in color
      PY_COLORS: 1
    defaults:
      run:
        # needed so micromamba activation from setup-micromamba takes effect
        shell: bash -el {0}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.14"]

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        # external/hdf5_io: CMake (CYTEN_WITH_HDF5) and the Python hdf5_io package
        submodules: true

    - name: Set up micromamba
      uses: mamba-org/setup-micromamba@v3
      with:
        environment-file: environment.yml
        # pin Python from the matrix; other deps (incl. CPU pytorch) come from environment.yml
        create-args: >-
          python=${{ matrix.python-version }}
        cache-environment: true

    - name: Build and install
      # build deps (pybind11, scikit-build-core, cmake, pytorch, ...) come from environment.yml
      # --no-build-isolation avoids pip pulling a CUDA torch wheel that needs the CUDA toolkit
      # hdf5_io is not on PyPI yet; install the git submodule first so cyten[io]/test] can resolve it
      run: |
        pip install --no-build-isolation -e ./external/hdf5_io
        pip install --no-build-isolation -e ".[test]"

    - name: Run pytest
      # configuration in pyproject.toml
      # Note: This runs in the repo root directory, which contains the cyten source folder.
      #       To use the version we just installed, it is important to run `pytest`
      #       instead of `python -m pytest`.
      run: pytest --block-backends "numpy" -vv

    - name: Check _core stub parity
      # Regenerates the autodoc stub from bindings and compares to the live extension.
      run: python scripts/check_core_stub_parity.py --generate
