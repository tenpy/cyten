# Conversion of ZN

See [convert_NoSymmetry.md](convert_NoSymmetry.md) (converted together with `NoSymmetry` and `U1` on branch `convert_NoSymmetry`).

Note: C++ `are_valid_sectors` requires `0 <= q < N` (fixes Python bug `np.all(0 < self.N)`).
